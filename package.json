{
  "name": "geez-offline-dictionary",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:dataset": "python3 tools/build_dataset.py",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}